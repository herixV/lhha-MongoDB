use ("utt_lhha_store")

db.products.updateOne(
    
    { _id: 2 },
    {
        // todo lo que llevue $nombre es un operador
        // todo lo que lleve nombre() es yn metodo de BD 
        $inc: {
            price: 121,
        }
    }
)